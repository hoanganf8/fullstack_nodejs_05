// export const getFlash = (cookies, key) => {
//   const data = cookies.get(key)?.value;
//   cookies.delete(key); //Xóa ngay cookie sau khi lấy được
//   return data;
// };

// export const setFlash = (cookies, key, value) => {
//   cookies.set(key, value, { path: "/" });
// };
